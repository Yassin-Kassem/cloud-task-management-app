{
  "name": "mini-jira-aws",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "create-tables": "npx tsx scripts/create-tables.ts",
    "create-tables-aws": "npx tsx scripts/create-tables-aws.ts",
    "seed": "npx tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1050.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.1050.0",
    "dotenv": "^16.4.0",
    "tsx": "^4.19.0"
  }
}
