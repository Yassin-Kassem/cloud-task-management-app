{
  "name": "image-resize-lambda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && cp package.json dist/ && cd dist && npm install --omit=dev"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "jimp": "^1.6.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.15.0",
    "typescript": "^5.6.0"
  }
}
